[package]
name = "chopin-orm"
version = "0.5.2"
edition = "2024"
license = "MIT"

[dependencies]
chopin-pg = { version = "0.5.2", path = "../chopin-pg" }
chopin-orm-macro = { path = "../chopin-orm-macro" }
async-trait = "0.1.86"
log = "0.4"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "orm_bench"
harness = false
